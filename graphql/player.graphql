query Player($username: String!){
  player(username: $username) {
    id
    name
    age
    username
    appearance
    answers {
      id
      question
      answer
      playerUsername
    }
  }
}